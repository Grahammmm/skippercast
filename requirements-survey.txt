# Optional original-BAG survey compiler, audited with CPython 3.12 on macOS.
# Use Python 3.12+ (CI: 3.13); the core application still supports Python 3.11.
# Do not upgrade geometry/native-array libraries without rerunning the scientific
# fixtures and comparing native BAG cells, masks, footprints and source hashes.
numpy==2.5.3
scipy==1.17.1
rasterio==1.5.1
shapely==2.1.2
pyproj==3.8.0
h5py==3.15.1
affine==3.0.1
pylerc==4.2.0
pyshp==3.1.6
